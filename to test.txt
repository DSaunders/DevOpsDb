Spikes that need testing:

- DevOps clients
- ResultTable: OnlyColumns



Queries I want to write:

    -- Get all builds for a project
    select * from devops.builds b
        inner join pipelines p on p.id = b.pipelineId
        inner join devops.projects project on project.id - p.projectId
        where project.name == "My project"